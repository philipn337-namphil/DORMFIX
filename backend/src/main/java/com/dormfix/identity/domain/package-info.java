/**
 * Users, roles and refresh-token session persistence under the accepted authentication contract.
 * Business invariants and value data; no Spring/web/infrastructure dependencies except JPA mapping annotations.
 * Repositories and authentication use cases are implemented by later vertical slices.
 */
package com.dormfix.identity.domain;
