# Include within nginx http {}, before the site config. Excludes URL queries and headers.
log_format dormfix '$remote_addr $request_method $uri $status $request_time '
                   'trace=$upstream_http_x_request_id';
