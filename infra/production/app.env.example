# Template only. Operator creates /etc/dormfix/app.env with mode 0600.
# Populate from approved secret storage; never commit the filled file.
DB_URL=jdbc:postgresql://REPLACE_RDS_HOST:5432/dormfix?sslmode=verify-full&sslrootcert=/run/certs/rds-ca.pem
DB_USERNAME=REPLACE_RUNTIME_DML_USER
DB_PASSWORD=
# No JWT configuration until the authentication contract is approved.
