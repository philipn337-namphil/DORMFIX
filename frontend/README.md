# Frontend boundary

Intentionally deferred. A minimal client will consume `/api/v1` DTOs and explicit commands after backend slices exist. No frontend framework, design system, AI capability, or generated mock business behavior is introduced in Phase 0.
