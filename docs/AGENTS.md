# Documentation scope

`product/` is the frozen authority captured from the 2026-09-10 foundation request. Do not silently reconcile gaps or import older rules from `context/`. Record issues in `architecture/open-questions.md` and proposed ADRs. Keep engineering decisions distinct from product facts. Accepted ADRs are superseded, not rewritten to conceal history. Ensure relative links and code/config references remain valid with `scripts/check_repository.py`.
